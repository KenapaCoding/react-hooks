import React from 'react';
import LoginButton from './LoginButton';

const Navbar = () => {
  return (
    <div>
      <ul>
        <LoginButton />
      </ul>
    </div>
  );
};

export default Navbar;
